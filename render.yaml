services:
  - type: worker
    name: polymarket-story-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python src/bot.py
    envVars:
      - key: DISCORD_WEBHOOK_URL
        sync: false
